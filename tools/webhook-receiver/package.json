{
  "name": "webhook-receiver",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel serverless functions that store raw GitHub webhook payloads in Supabase.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.6.0",
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
